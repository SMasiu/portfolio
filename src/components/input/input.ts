import styled from 'styled-components'

export const Input = styled.input`
  width: 50px;
  border-radius: 5px;
  border: none;
  padding: 10px;
  text-align: center;

  &:focus {
    outline: none;
  }
`
